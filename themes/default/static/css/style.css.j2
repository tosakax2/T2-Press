{% set stripe_background = "repeating-linear-gradient(45deg, " ~
    theme.colors.stripe_background1 ~ ", " ~
    theme.colors.stripe_background1 ~ " 2px, " ~
    theme.colors.stripe_background2 ~ " 2px, " ~
    theme.colors.stripe_background2 ~ " 4px)" %}

/* ==========================================
   RESET CSS（完全リセット）
   ========================================== */

/* ボックスサイズを border-box に統一 */
*, *::before, *::after {
    box-sizing: border-box;
}

/* 全てのマージン・パディングをゼロに */
body, h1, h2, h3, h4, h5, h6, p, blockquote, pre,
dl, dd, ol, ul, figure, hr, fieldset, legend,
table, th, td, input, button {
    margin: 0;
    padding: 0;
}

th, td {
    border: none;
    text-align: left;
}

/* リストのマーカーを削除 */
ul, ol {
    list-style: none;
}

/* 引用符・イタリックなどのブラウザ装飾を削除 */
blockquote, q {
    quotes: none;
}

blockquote::before, blockquote::after,
q::before, q::after {
    content: '';
}

/* テーブルのボーダー間隔をリセット */
table {
    border-collapse: collapse;
    border-spacing: 0;
}

/* HTML5要素を block として扱う（古いIE対策） */
article, aside, details, figcaption, figure,
footer, header, main, nav, section, summary {
    display: block;
}

/* フォーム要素の見た目をリセット */
input, textarea, select, button {
    font: inherit;
    color: inherit;
    background: none;
    border: none;
    outline: none;
    appearance: none;
}

/* 強調・リンク色を初期化 */
a {
    color: inherit;
    text-decoration: none;
}

em, i, strong, b {
    font-style: normal;
    font-weight: normal;
}

/* 画像の隙間対策（下に余白ができる問題） */
img {
    vertical-align: bottom;
    max-width: 100%;
    height: auto;
    display: block;
}

/* ボディの基本フォント設定 */
body {
    line-height: 1;
    font-family: sans-serif;
    background: #fff;
    color: #000;
}
/* ========================================== */

/* --- サイト全体 --- */
html {
    font-size: 16px;
}
body {
    font-family: {{ theme.fonts.base }};
    background-color: {{ theme.colors.master_background }};
    color: {{ theme.colors.master_text }};
    font-size: 1rem;
    line-height: 1.6;
}
html, body {
    height: 100%;
    margin: 0;
    padding: 0;
}
.site-wrapper {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}
.site-container {
    flex: 1;
}

/* --- サイト全体のナビゲーション --- */
.site-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 64px;
    background: {{ stripe_background }};
    overflow: hidden;
}
.site-nav-loader {
    position: absolute;
    bottom: 2px;
    left: 0;
    height: 3px;
    width: 0%;
    background-color: {{ theme.colors.master_accent }};
    z-index: 0;
    transition: width 0.3s ease;
}
.site-nav-link {
    position: relative;
    display: flex;
    bottom: 2px;
    align-items: center;
    justify-content: center;
    height: 100%;
    padding: 0 1.5rem;
    box-sizing: border-box;

    font-size: 1rem;
    font-weight: bold;
    color: {{ theme.colors.stripe_text }};
    text-decoration: none;
}
.site-nav-link:hover {
    background-color: {{ theme.colors.master_accent }};
    color: {{ theme.colors.stripe_background1 }};
}
.site-nav-link > .material-icons-outlined {
    font-size: 1.35rem;
    margin-right: 0.5rem;
}

/* --- サイト全体のヘッダー --- */
.site-header {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 64px;
    min-height: 400px;

    color: {{ theme.colors.master_text }};
}
.site-header-title {
    font-size: 2.5rem;
    font-weight: bold;
    line-height: 1.3;
}

/* --- サイト全体のメインコンテンツ --- */
.site-container {
    display: flex;
    justify-content: center;
    margin: 0 auto;
    width: 100%;
}
.site-content {
    display: flex;
    flex-direction: row;
    justify-content: center;
    margin: 0 16px;
    width: 1280px;
}
.sidebar {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-right: 32px;
    width: 25%;

    background-color: {{ theme.colors.container_background }};
}
.site-main {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 75%;

    background-color: {{ theme.colors.container_background }};
}

/* --- サイト全体のフッター --- */
.site-footer {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin-top: 32px;
    padding: 1rem;
    width: 100%;
    background: {{ stripe_background }};
}
.site-footer-text {
    font-size: 0.8rem;
    line-height: 2.0;
    color: {{ theme.colors.stripe_text }};
}

/* --- 見出し --- */
h1 {
    font-size: 2.25rem;
    font-weight: bold;
    line-height: 1.3;
    text-align: center;
}
h2 {
    margin: 2rem 0 1rem;
    padding: 0.5em;
    border-left: solid 8px {{ theme.colors.master_accent }};
    border-radius: 8px 2px 2px 8px;

    font-size: 1.75rem;
    font-weight: bold;
    line-height: 1.35;
}
h3 {
    font-size: 1.25rem;
    font-weight: bold;
    line-height: 1.4;
    margin: 1.5rem 0 0.75rem 1rem;
}

/* --- 本文 ---*/
p {
    font-size: 1rem;
    line-height: 1.6;
    margin: 0 1.5rem;
}

/* --- リンク --- */
a {
    color: {{ theme.colors.link_text }};
    text-decoration: none;
}

/* --- リスト --- */
ul {
    list-style: disc;
    font-size: 1rem;
    padding-left: 1.5rem;
}
ol {
    list-style: decimal;
    font-size: 1rem;
    padding-left: 1.5rem;
}
ol ::marker {
    font-weight: bold;
}

/* --- 区切り線 --- */
hr {
    border: none;
    border-top: 1px solid {{ theme.colors.divider }};
    margin: 2rem 0;
    width: 100%;
}

/* --- コードインライン --- */
code {
    font-family: {{ theme.fonts.code }};
    background-color: {{ theme.colors.code_background }};
    color: {{ theme.colors.code_text }};
    font-size: 0.85rem;
    line-height: 1.4;
    border-radius: 4px;
    padding: 0.2rem;
}

/* --- コードブロック --- */
pre {
    font-family: {{ theme.fonts.code }};
    background-color: {{ theme.colors.code_block_background }};
    color: {{ theme.colors.code_block_text }};
    font-size: 0.9rem;
    line-height: 1.5;
    border-radius: 4px;
    overflow-x: auto;
    padding: 0.85rem 0.9rem;
}

/* --- テーブル --- */
table {
    border: solid 2px {{ theme.colors.table_border }};
    margin: 1.5rem;
}
th {
    background-color: {{ theme.colors.table_header_background }};
    font-size: 0.9rem;
    font-weight: bold;
    border: solid 1px {{ theme.colors.table_border }};
    padding: 0.25rem 0.5rem;
    min-width: 5rem;
}
td {
    background-color: {{ theme.colors.table_background }};
    font-size: 0.9rem;
    border: solid 1px {{ theme.colors.table_border }};
    padding: 0.25rem 0.5rem;
    min-width: 5rem;
}
.corner-cell {
    position: relative;
    background-color: {{ theme.colors.table_header_background }};
    text-align: center;
    vertical-align: middle;
    font-weight: bold;
    padding: 0;
    overflow: hidden;
}
.corner-cell .corner-line {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
}
.corner-cell > *:not(svg) {
    position: relative;
    z-index: 1;
}

/* --- 画像 ---*/
img {
    border-radius: 8px;
    margin: 2rem auto;
    display: block;
}

/* --- 動画 ---*/
iframe {
    border-radius: 8px;
    margin: 2rem auto;
    display: block;
}